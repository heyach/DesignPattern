### 设计模式 同步模块模式
通过模块管理器定义多个不同高内聚低耦合的模块（可以分开进行），引用的时候以同步的方式，立即执行回调

### 一句话
先一个个的定义好模块，然后引入使用

### 定义模块
首先定义一个模块管理器，然后实现一个定义模块的方法
```js
var S = {}
S.define = function(module, fn) {
    // 
}
```

### 使用模块
```js
S.module(["module1", "module2"], function(module1, module2) {
    //
})
```